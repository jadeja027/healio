# Copy to .env and set your key:
#   cp .env.example .env
#
# Database (pick one):
# - Omit DATABASE_URL → SQLite file at backend/healio.db (no Docker).
# - Postgres: DATABASE_URL=postgresql+psycopg://postgres:postgres@localhost:5432/healio
#   (run: docker compose up -d from repo root)
#
# Gemini (Google AI Studio): https://aistudio.google.com/apikey
# Use either GEMINI_API_KEY or GOOGLE_API_KEY (same key from AI Studio).

GEMINI_API_KEY=AIzaSyDDCpPQWzIzVA6yJbAkoBWNz73UyZlka3A
# GOOGLE_API_KEY=
# GEMINI_MODEL=gemini-2.0-flash
